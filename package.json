{
  "name": "oh-node",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "nodemon -e ts --exec \"npm run build\"",
    "build": "tsc --build src/tsconfig.json && ./build.sh",
    "test": "jest --coverage --verbose --colors --silent",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "author": "0xMatt",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.1.0",
    "path-to-regexp": "^2.4.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.7",
    "@types/node": "^10.12.0",
    "jest": "^23.6.0",
    "lerna": "^3.8.0",
    "supertest": "^3.3.0",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "typescript": "^3.1.3"
  }
}
